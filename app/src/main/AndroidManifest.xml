<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.psmupdate">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoreal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PSMUpdateNoStatusBar">

        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.PSMUpdate"
            android:windowSoftInputMode="stateVisible"
            >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity android:name=".ResultatsQuizz"
            android:label="Résultats"
            android:theme="@style/Theme.PSMUpdate"
            />

        <activity android:name=".Questions"
            android:label="Quiz"
            android:theme="@style/Theme.PSMUpdate"
            />

        <activity
            android:name=".MainActivity"
            android:label="Accueil"
            android:theme="@style/Theme.PSMUpdate"
            android:windowSoftInputMode="stateVisible">
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>